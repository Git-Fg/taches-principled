# Phase 0: Plugin Scaffold

## Objective
Create the directory structure, plugin manifests, and marketplace configuration for all 11 imported plugins. After this phase, each plugin has an empty skeleton ready for skill implementation.

**Policy:** Framework before content. All targeting the `tp-` namespace prefix.

## Context
@.principled/plans/BRIEF.md
@.principled/plans/ROADMAP.md
@.principled/scratch/integration-architecture.md

## Tasks

### Task 1: Create all plugin directories and manifests
Files: plugins/{tp-sadd,tp-sdd,tp-reflexion,tp-kaizen,tp-fpf,tp-review,tp-docs,tp-git,tp-tdd,tp-ddd,tp-tech-stack}/.claude-plugin/plugin.json
Action: For each plugin, create `plugins/{name}/.claude-plugin/plugin.json` with:
- name: `tp-{name}`
- version: `0.1.0` (fresh start — these are taches-principled rewrites, not CEK ports)
- author: Felix
- Standard plugin.json fields (displayName, description, license)
- Keyword: `taches-principled` for discoverability

Verify: Each `plugin.json` is valid JSON and contains required fields
Done: 11 plugin manifests created

### Task 2: Create empty skill directory stubs
Files: plugins/{tp-sadd,tp-sdd,...}/skills/
Action: For each plugin that will have skills, create empty directories:
- tp-sadd: judge, do-in-steps, do-in-parallel, do-competitively, tree-of-thoughts, judge-with-debate, launch-sub-agent, subagent-driven-development, do-and-judge, multi-agent-patterns
- tp-sdd: brainstorm, create-ideas, add-task, plan-task, implement-task
- tp-reflexion: reflect, critique, memorize
- tp-kaizen: kaizen, analyse, why, cause-and-effect, plan-do-check-act, root-cause-tracing, analyse-problem
- tp-fpf: query, status, reset, actualize, decay, propose-hypotheses
- tp-review: review-pr, review-local-changes
- tp-docs: update-docs, write-concisely  
- tp-git: load-issues, analyze-issue, commit, create-pr, attach-review-to-pr, git-notes, git-worktrees
- tp-tdd: write-tests, test-driven-development, fix-tests
- tp-ddd: (rules only, no skills)
- tp-tech-stack: (rules only, no skills)

Each directory gets a `.gitkeep` placeholder.

Verify: `find plugins -type d | sort` shows expected structure
Done: 46 skill directories created

### Task 3: Create agent directories for plugins with agents
Files: plugins/{tp-sadd,tp-sdd,tp-review}/agents/.gitkeep
Action: Create `agents/` directories in plugins that use bundled agents:
- tp-sadd: agents/ (judge, meta-judge)
- tp-sdd: agents/ (business-analyst, code-explorer, developer, qa-engineer, researcher, software-architect, team-lead, tech-lead, tech-writer)
- tp-review: agents/ (bug-hunter, code-quality-reviewer, contracts-reviewer, historical-context-reviewer, security-auditor, test-coverage-reviewer)

Verify: `find plugins -path "*/agents/*" -type f | sort` shows .gitkeep files
Done: 17 agent directories created

### Task 4: Update root marketplace.json
Files: .claude-plugin/marketplace.json
Action: Add all 11 plugins to the marketplace.json plugins array. Each entry:
```json
{
  "name": "tp-{name}",
  "source": { "source": "url", "url": "https://github.com/Git-Fg/taches-principled.git" },
  "description": "[one-line routing description]",
  "version": "0.1.0"
}
```

Verify: marketplace.json is valid JSON, all 12 plugins listed (taches-principled + 11 new)
Done: Marketplace lists 12 plugins

### Task 5: Create reference template for skills
Files: plugins/tp-reflexion/skills/SKILL_TEMPLATE.md
Action: Write a SKILL.md template that all imported skills will follow:
```markdown
---
name: {name}
description: {routing-optimized description — what triggers this, in 150 chars}
argument-hint: {usage hint}
---

## Decision Router

IF {trigger condition} → {first action}
IF {secondary condition} → {alternative action}
IF {combining with other plugins} → {synergy hint}

# {Skill Name}

{One paragraph: what this skill does, when to use it, what it produces}

## Core Principle

{The single invariant this skill enforces}

## {Workflow/Process}

{What to do, step by step — NOT how tools work, but what outcomes to produce}

## Output

{What the skill produces and where it goes}
```

Verify: Template reads as standalone instruction
Done: Template in place

### Task 6: Update root CLAUDE.md with multi-plugin management
Files: CLAUDE.md
Action: Add plugin management section documenting:
- Marketplace structure: plugins/ directory
- Version pattern: each plugin independently versioned
- Adding a new plugin: scaffold → write skills → update marketplace → push
- Naming convention: `tp-{name}` for all imported plugins

Verify: CLAUDE.md reads as complete management guide
Done: Development guide updated

## Verification
- [ ] `find plugins -name "plugin.json" | wc -l` = 12 (core + 11 imported)
- [ ] `find plugins -name ".gitkeep" | wc -l` = >60 (all skill + agent dirs)
- [ ] marketplace.json lists 12 plugins
- [ ] Template file is valid markdown
- [ ] CLAUDE.md has plugin management section

## Success Criteria
- Every imported plugin has an empty skeleton ready for implementation
- Marketplace can distribute all plugins
- Template exists for consistent SKILL.md authoring

## Output
After completion, create `.principled/plans/phases/00-scaffold/SUMMARY.md`
