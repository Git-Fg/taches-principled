---
name: capture
description: "Run a headless Claude Code session with canonical behavioral capture flags and return artifact paths"
skill: session-analytics
argument-hint: "<prompt-to-execute> [--budget 0.50]"
---

$ARGUMENTS

Run a headless capture of Claude Code behavior using canonical verification flags. Use the session-analytics skill in CAPTURE mode if you have access to it.